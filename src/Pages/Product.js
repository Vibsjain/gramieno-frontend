import React from "react";
import { Navbar, Footer } from "../Components";

const Product = () => {
    return (
        <div className="back">
            <Navbar />
            <Footer />
        </div>
    );
};

export default Product;
