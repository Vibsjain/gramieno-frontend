import React from "react";
import { Navbar, Footer } from "../Components";

const Cart = () => {
    return (
        <div className="back">
            <Navbar />
            <Footer />
        </div>
    );
};

export default Cart;
